# Ingredient Scanner (Expo)

## Quick start
```bash
npm install
npx expo install expo-camera
npx expo start
```

## Build APK
```bash
npm install -g eas-cli
eas login
eas build -p android --profile preview
```
